# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES FOR VERCEL
# ===========================================
# Copy these to your Vercel project settings:
# https://vercel.com/[your-username]/xpandorax-com/settings/environment-variables

# MongoDB Connection (REQUIRED)
# Get this from MongoDB Atlas > Connect > Drivers
# Make sure to URL-encode special characters in password (! = %21, @ = %40, etc.)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/xpandorax?retryWrites=true&w=majority

# Payload CMS Secret (REQUIRED)
# Generate a secure random string (32+ characters)
# You can use: openssl rand -base64 32
PAYLOAD_SECRET=your-super-secure-random-string-here-change-this

# Site URLs (REQUIRED for production)
NEXT_PUBLIC_SITE_URL=https://xpandorax.com
NEXT_PUBLIC_SERVER_URL=https://xpandorax.com

# Cloudflare R2 Storage (REQUIRED for uploads)
R2_ACCOUNT_ID=your_r2_account_id
R2_BUCKET=your_bucket_name
R2_ACCESS_KEY_ID=your_access_key_id
R2_SECRET_ACCESS_KEY=your_secret_access_key
R2_REGION=auto
R2_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com
NEXT_PUBLIC_R2_URL=https://your-public-bucket-url.r2.dev

# Logo URL (optional - uses R2 public URL)
NEXT_PUBLIC_LOGO_URL=https://your-public-bucket-url.r2.dev/logo/xpandorax_logo.png

# ===========================================
# IMPORTANT SETUP STEPS FOR PRODUCTION
# ===========================================
#
# 1. MONGODB ATLAS IP WHITELIST:
#    - Go to MongoDB Atlas > Network Access
#    - Add 0.0.0.0/0 (Allow from anywhere) for Vercel
#    - Or add Vercel's IP ranges if you need more security
#
# 2. VERCEL ENVIRONMENT VARIABLES:
#    - Go to Vercel Dashboard > Project > Settings > Environment Variables
#    - Add ALL the variables above
#    - Make sure to set them for Production AND Preview environments
#
# 3. REDEPLOY AFTER ADDING VARIABLES:
#    - After adding environment variables, trigger a new deployment
#    - Go to Deployments tab > Click "..." on latest > Redeploy
#
# 4. CHECK FUNCTION LOGS:
#    - If still getting errors, check Vercel Function Logs
#    - Go to Vercel Dashboard > Project > Logs
