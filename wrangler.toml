name = "xpandorax-com"
pages_build_output_dir = "./build/client"
compatibility_date = "2024-12-01"

# Local development only - for running `wrangler pages dev`
# Cloudflare Pages ignores this and uses its own settings
[build]
command = "npm run build"
cwd = "./"